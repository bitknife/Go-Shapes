# Python clients

## Developer setup
Using pynev as virtual environment manager:

MAC:

    brew install pyenv

Ubuntu:

    apt install pyenv    

And then:

    pyenv install 3.10
    pyenv virtualenv 3.10 wtf
    pyenv activate wtf
    pip install -r requirements.txt